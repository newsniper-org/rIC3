[package]
name = "rIC3"
version = "1.5.2"
edition = "2024"
authors = ["Yuheng Su <gipsyh.icu@gmail.com>"]
repository = "https://github.com/gipsyh/rIC3"
description = "hardware model checker"
keywords = ["formal-method", "model-checking", "IC3"]
license = "GPL-3.0"

[[bin]]
name = "ric3"
path = "src/main.rs"

[dependencies]
aig = { path = "./deps/aig-rs", version = "0.4.6" }
cadical = { package = "satif-cadical", path = "./deps/cadical-rs", version = "0.2.2" }
kissat = { package = "satif-kissat", path = "./deps/kissat-rs", version = "0.5.2" }
bitwuzla = { package = "smtif-bitwuzla", path = "./deps/bitwuzla-rs", version = "0.1.0" }
logicrs = { path = "./deps/logicrs", version = "0.6.1" }
giputils = { path = "./deps/giputils", version = "0.3.6" }
btor = { path = "./deps/btor-rs", version = "0.2.2" }
clap = { version = "4.5.60", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
rand = "0.10.1"
ctrlc = { version = "3.4.1" }
bitfield-struct = "0.13.0"
tempfile = "3.25.0"
nix = { version = "0.31.1", features = ["signal", "term"] }
env_logger = "0.11.10"
log = "0.4.32"
time = { version = "0.3.46", features = ["formatting", "macros"] }
toml = "1.1.2"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
sha2 = "0.11"
ratatui = "0.30.0"
anyhow = "1.0.101"
ron = "0.12.0"
regex = "1.12.3"
strum = { version = "0.28", features = ["derive"] }
tabled = { version = "0.21.0", features = ["ansi"] }
vcd = "0.7.0"
enum-as-inner = "0.7.0"
rayon = "1.10.0"
fork = "0.8.0"
ipc-channel = "0.22.0"
intertrait = "0.2.2"
linkme = "0.2.10"
rmcp = { version = "1.7.0", features = ["transport-io"] }
tokio = { version = "1.52.3", features = [
    "rt-multi-thread",
    "macros",
    "io-std",
] }
nalgebra = "0.35.0"
memory-stats = "1.2.0"

[profile.release]
lto = true
panic = "abort"
strip = true

# Profiling profile for AGENTS.md §1.3, which requires `perf` self-time on
# "release + debuginfo". [profile.release] sets strip = true, so perf sees no
# symbols and the five-shape profile cannot be produced from it.
#
# lto and panic are inherited from release deliberately. LTO changes inlining,
# so disabling it would make self-time attribution unrepresentative of the
# build we actually ship. Any profile figure MUST be reported together with the
# profile it was taken under.
#
# Build with: cargo build --profile profiling
# Binary at:  target/profiling/ric3
[profile.profiling]
inherits = "release"
debug = true
strip = false

# [profile.dev]
# opt-level = 3
